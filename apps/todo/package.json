{
  "name": "todo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:watch": "run-p build:watch:api",
    "build:watch:api": "cd packages/api && npm run build:watch",
    "build": "npm run build:api",
    "build:api": "cd packages/api && npm run build",
    "emulate": "firebase emulators:start",
    "build-web": "cd packages/web && npm run build",
    "run-web": "cd packages/web && npm run start",
    "dev": "npm run build && run-p build:watch emulate run-web",
    "deploy:functions": "node deploy.js functions",
    "deploy:hosting": "node deploy.js hosting",
    "deploy:firestore": "firebase deploy --only firestore",
    "deploy:all": "firebase deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase-functions": "^5.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "inquirer": "^9.3.4",
    "npm-run-all": "^4.1.5",
    "firebase-tools-with-isolate": "^13.11.2"
  }
}
